"""Guardrails package.

Define input validation guardrails here.

Example:
    def validate_input(ctx, input_data: dict) -> tuple[bool, str]:
        '''Validate input data.
        
        Returns:
            (is_valid, error_message)
        '''
        if not input_data.get("required_field"):
            return False, "required_field is missing"
        return True, ""
"""

__all__ = []
